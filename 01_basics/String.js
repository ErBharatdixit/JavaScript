const name = "bharat dixit"

const repoCount = 50

console.log(`hello my name is ${name} and my repo count is ${repoCount}`);

const Sname = new String("dixit")

console.log(Sname[0]);
console.log(Sname.__proto__);
console.log(Sname.length);
console.log(Sname.toUpperCase());
// we have a more string functions like trim,space,substring, slice, replace etc.


